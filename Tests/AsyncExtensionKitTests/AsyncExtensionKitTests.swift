import XCTest
@testable import AsyncExtensionKit

final class AsyncExtensionKitTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(AsyncExtensionKit().text, "Hello, World!")
    }
}
